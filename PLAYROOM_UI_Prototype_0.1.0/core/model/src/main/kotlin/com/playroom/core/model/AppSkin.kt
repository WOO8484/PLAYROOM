package com.playroom.core.model

/** App-wide color skin selectable from 설정 › 앱 스킨. */
enum class AppSkin(val label: String) {
    LIGHT("PLAYROOM 라이트"),
    DARK("PLAYROOM 다크"),
    RETRO("레트로"),
    SIMPLE("심플"),
}
