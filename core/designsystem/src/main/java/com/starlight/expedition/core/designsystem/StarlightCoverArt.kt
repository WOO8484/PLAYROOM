package com.starlight.expedition.core.designsystem

/**
 * 빠른 시작 화면 커버 프레임에서 사용하는 두 대표 이미지의 drawable 리소스 id입니다.
 * (실행지시서 19절) 네트워크 이미지를 쓰지 않고, 두 PNG 파일만 로컬 자산으로 사용합니다.
 */
object StarlightCoverArt {
    val starlight: Int = R.drawable.cover_starlight
    val heroLegend: Int = R.drawable.cover_hero_legend
}
